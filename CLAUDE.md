# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This is a newly initialized, empty repository (`ey-polaris`) with no commits yet. The remote is `https://github.com/faysia320/ey-polaris.git`.

There is no build system, language toolchain, or source code to document yet. Once the project takes shape, re-run `/init` to regenerate this file with build/test commands and architecture notes.
