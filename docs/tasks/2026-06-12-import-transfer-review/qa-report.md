# QA Report: 엑셀 업로드 이체 행 검토·분류 흐름 (+ 선행 transfer 거래 도입)

- 날짜: 2026-06-12
- 작업 폴더: `docs/tasks/2026-06-12-import-transfer-review` (선행 명세 `docs/tasks/2026-06-12-transfer-transactions` 포함 평가)
- 판정: **CONDITIONAL PASS**
- 평가 대상 커밋: `7ed59b3` (평가 진행 중 커밋됨 — 검토한 diff와 동일 내용, 평가 후 작업 트리 무변경 확인)

## 성공 기준 채점 (본 명세)

- ✅ AC-1: 미리보기 DB 무변경 + 검토 행 전부 반환 — 실제 xlsx로 `POST /transactions/import/preview`(2026-01) 호출. 월 63행 = importable 57 + review 6 + skipped 0으로 이체 타입 전부가 검토 대상. 행번호·날짜·원본 분류·내용·부호 금액·결제수단·기본 제안 모두 포함. 호출 전후 총 거래 250건 무변화.
- ✅ AC-2: 내계좌이체 자동 페어 — 431행(WON 통장 +18,000)·432행(카카오페이 머니 −18,000)이 상호 `pair_row`로 제안되고, 확정 시 "카카오페이 머니 → WON 통장" 18,000원 **단일** transfer 거래(카테고리 '이체 > 내계좌이체')로 적재됨을 거래 목록으로 확인.
- ✅ AC-3: 행별 결정 반영 — 혼합 결정(페어 이체 2행→1건, 433행 지출 전환, 459행 건너뛰기, 460행 명시 이체 상대=카드, 490행 결정 없음)으로 확정. created 60(=57+1전환+2이체), transfer_count 2, converted_count 1, 건너뜀 2건(사유 구분: "검토에서 건너뜀" / "검토 결정 없음 — 건너뜀"). `/analytics/dashboard` 수입/지출 증가분이 적재된 income/expense 합과 정확히 일치(4,350,636 / 2,936,005), 이체는 제외. `/analytics/assets` grand_total 변화(+1,414,631)가 수입−지출과 일치(이체 양다리 상쇄). 카드 계정(14)에 이체 입금 다리 +1,970,940 반영 확인(정리 후 전 계정 잔액 원복으로 역검증).
- ✅ AC-4: 원자성 — ① 페어 해제 후 상대 누락(422 "431행: 이체 결정에는 상대 계정이 필요합니다"), ② 상대=결제수단 동일(422), ③ 존재하지 않는 상대 id(422), ④ decisions JSON 깨짐(422). ②③은 교체 삭제·일반 행 적재가 세션에 누적된 **후** 예외가 발생하는 경로인데도 총 거래 250건 무변화 — `get_db`의 `close()` 롤백으로 원자성 성립 확인.
- ✅ AC-5: 이체 행 없는 월 — 실제 파일은 모든 월에 이체 행이 있어, 이체 없는 픽스처 xlsx(컨테이너 임시 경로, 검증 후 삭제)로 확인: preview review=0, `decisions=[]` 확정 시 기존 흐름과 동일하게 2건 적재. 프론트 분기(`preview.review.length === 0 → commitImport` 즉시 확정)는 코드로 확인.
- ✅ AC-6: 재업로드 교체 — 1차 확정(60건) 후 수동 거래 1건 추가, 동일 결정 재업로드 → deleted 60(transfer 임포트 포함)·created 60, 수동 거래("QA manual keep") 보존, 월 합계 61건.
- ✅ AC-7: 검토 UI — API로 카드대금 행(460) 기본 제안 `transfer` 확인. 행별 4종 Select·페어 자동 표기·카드대금 지출 경고(`major==='카드대금' && action==='expense'` 조건 렌더)는 코드로 확인, `npm run build` 통과. 단, 실제 브라우저(:3000) 조작은 본 평가 환경에 브라우저 도구가 없어 수행하지 못함(아래 Medium 이슈).
- ⚠️ AC-8 (모바일 375px): **동적 미검증** — 브라우저 도구 부재. 정적 점검 결과 위험 신호 없음: DialogContent `max-w-[calc(100%-2rem)]`(375px에서 343px), 검토 행 헤더 `flex-wrap`, 결정 컨트롤 행 `flex-wrap` + 고정폭 합(w-28 112px + w-44 176px + gap 8px = 296px)이 내부 가용폭(~311px) 이내, description `truncate`, 목록 `max-h-[50vh] overflow-y-auto`. 구현 측도 343px 강제 구조 검사만 수행(implementation.md 보류 항목). 정황상 충족으로 보나 AC가 요구한 브라우저 확인 증거가 없어 Medium으로 기록.
- ✅ AC-9: 전환 흔적 — 지출 전환 행 memo가 `이*혜 [이체→지출: 이체]`로 원본 분류를 보존함을 거래 상세로 확인.

## 성공 기준 채점 (선행 명세, 유효 항목)

- ✅ AC-1/2: 수동 transfer 201 → 출금 계정 −50,000 / 입금 +50,000, grand_total 불변, 대시보드 income/expense 불변 — 등록 전후 API 비교.
- ✅ AC-3/5: member1 계정→member2 계정 이체 30,000 → `assets?member_id=1` total −30,000, `member_id=2` total +30,000, trend 마지막 포인트 동일 이동 — 확인 후 삭제·원복.
- ✅ AC-4: 422 4종(상대 누락 / 출금=입금 / 수입·지출에 상대 지정 / transfer에 expense 카테고리) 전부 한국어 사유와 함께 422.
- ✅ AC-8: `GET /transactions?kind=transfer` 정확히 이체 2건 반환.
- ✅ AC-9: 스택 기동 중(alembic 0006 적용 완료), `GET /categories`에 kind=transfer 5종('이체' 대분류: 내계좌이체/카드대금/저축/투자/미분류) 시드, 기존 집계 무변(검증 전후 총 250건·grand_total −17,303,649 원복).
- ⚠️ AC-6/7 (폼 등록/표시, 캘린더 제외): 직렬화(`counter_account_id/name`)·테이블 "출금 → 입금" 표기·캘린더 transfer 제외 분기는 코드 확인 + 빌드 통과 + API 직렬화 확인. 브라우저 육안 확인은 도구 부재로 미수행(AC-7/8 Medium에 포함).
- ✅ AC-10: 본 명세 검토 흐름으로 대체됨 — 이체 행이 스킵이 아닌 검토 대상으로 반환됨을 AC-1에서 확인.

## 검증 시나리오

- `docker compose` 스택(backend :8000) 상대로 실제 xlsx(`2025-06-11~2026-06-11.xlsx`) 사용. 테스트 월은 기존 거래 0건인 2026-01만 사용, 모든 생성 데이터(거래 121건 누적, 자동 생성 카테고리 1종, 수동 거래 2건) 검증 후 삭제 — 최종 상태: 총 거래 250건, 카테고리 66종, 계정 11개, grand_total −17,303,649, 대시보드 2026-01 = 0/0 **전부 기준선 원복 확인**. `git status` 무변경.
- 엣지 케이스: 존재하지 않는 월(422), month 형식 오류(422), 비-xlsx 파일(422), decisions 깨진 JSON(422), 페어 한쪽 skip으로 자동 해제(422), 상대=자기 계정(422), 미존재 상대 계정(422), 결정 누락 행(보수적 스킵), 이체 행 없는 파일(검토 생략).
- 정적: 변경 14파일 + 신규 마이그레이션 전체 정독. `python -m py_compile` 6파일 통과, `npm run lint` 0 errors(경고 2건은 기존 useReactTable 호환성), `npm run build` 통과(번들 500kB 경고는 기존).
- 실행 불가 항목: 브라우저 UI 조작(이 평가 환경에 브라우저 도구 없음) — AC-7 렌더 육안·AC-8 375px 뷰포트는 정적 분석으로 대체.

## 발견 이슈

- [Medium] `frontend/src/pages/TransactionsPage.tsx:914-1012` (검토 다이얼로그) — AC-8이 요구한 **실제 375px 뷰포트 동적 확인이 구현·QA 양쪽 모두 미수행**(구현은 343px 강제 구조 검사로 대체, QA는 브라우저 도구 부재). 정적 점검(flex-wrap·max-w·truncate·고정폭 합 296px < 343px)으로는 오버플로 징후가 없으나, AC가 명시한 증거(브라우저 375px 확인)가 없다. devtools 기기 에뮬레이션(375×812)으로 업로드→검토→결과 3단계 화면 확인 필요.
- [Low] `backend/app/excel_import.py:202-222` — `_pair_own_transfers`가 탐욕적 첫-일치 매칭이라, 같은 날짜·같은 금액의 내계좌이체가 서로 다른 계정 쌍으로 2건 이상 존재하면(예: A→B 1만, B→C 1만) 교차 오페어링이 가능하다. 제안일 뿐이고 UI에 페어 상대가 표시되어 사용자가 해제할 수 있으며 실제 데이터에서는 단일 패턴(카카오페이↔WON)이라 영향은 제한적.
- [Low] `backend/app/routers/transactions.py:292-319` — decisions의 행 번호가 검토 대상에 없으면 조용히 무시되고, `income`/`expense` 결정에 `counter_account_id`가 실려 와도 무시된다(수동 API `_validate_refs`는 동일 상황을 422로 거부 — 검증 강도 비대칭). 클라이언트 버그를 가릴 수 있으나 데이터 정합성은 해치지 않음.

## 수정 Action Items (CONDITIONAL)

- [ ] 브라우저 devtools 기기 에뮬레이션(375×812)으로 업로드 다이얼로그 3단계(파일 선택 → 이체 내역 검토 → 결과)에서 가로 스크롤·겹침·잘림 없음을 확인하고 증거(스크린샷 또는 확인 기록)를 남길 것 — 코드 수정이 필요 없다면 확인 기록만으로 종결 가능.
- [ ] (선택, Low) `/import` 확정에서 검토 대상에 없는 행 번호의 결정을 422로 거부해 클라이언트 버그를 조기 노출.

## 다음 단계

`/implement docs/tasks/2026-06-12-import-transfer-review` 로 Medium(375px 동적 확인) 해소 후 `/qa` 재실행. (백엔드·API 계약은 전 항목 동적 검증 통과 — 잔여 리스크는 모바일 뷰포트 확인뿐이므로, 사용자가 직접 375px 확인으로 갈음하는 판단도 가능)
