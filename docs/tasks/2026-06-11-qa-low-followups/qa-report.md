# QA Report: 이전 사이클 QA Low 이슈 3건 후속 개선

- 날짜: 2026-06-11
- 작업 폴더: docs/tasks/2026-06-11-qa-low-followups
- 판정: PASS

## 성공 기준 채점

- ✅ AC-1: 평가액 빈 제출 차단 — 브라우저(localhost:3000)에서 평가액 갱신 다이얼로그를 열고 금액 필드를 비운 채 "기록" 클릭. 다이얼로그 안에 "평가액을 입력해주세요" 에러가 표시되고, 네트워크 추적(read_network_requests, 추적 활성 상태)에서 `/api/` 요청 0건 확인 — API 호출 미발생.
- ✅ AC-2: 명시적 0원 허용 — UI에서 평가액 `0` 입력 후 기록: `PUT /api/v1/accounts/12/valuations` 200, 계정 카드가 "0원 / 평가 기준일 2026-06-11"로 갱신됨을 화면으로 확인.
- ✅ AC-3: 이력 표시 — 임시 주식 계정(API로 생성)에 2026-06-01(5,000,000), 2026-06-10(7,000,000) 두 건 기록 후 다이얼로그 오픈: "평가 이력" 섹션에 날짜 내림차순(06-10 → 06-01)으로 기준일·금액·삭제 버튼 표시 확인.
- ✅ AC-4: 개별 삭제 + 잔액 갱신 — 최신(06-10) 항목의 휴지통 클릭: DELETE → GET valuations → GET /analytics/assets 순 호출, 이력이 1건으로 줄고 카드 잔액 7,000,000→5,000,000원, 평가 기준일 2026-06-01로 즉시 갱신, 총자산 카드도 동시 갱신 확인. (참고: 크롬 확장 네트워크 로그가 DELETE를 503으로 표기했으나 서버 직접 조회로 실제 삭제 성공(재삭제 시 404) 확인 — 204 응답에 대한 추적기 표기 한계)
- ✅ AC-5: 전체 삭제 시 거래 기반 복귀 — 남은 1건 삭제 후 카드 잔액이 개설잔액 1,000,000원으로 복귀, "평가 기준일" 표기 소멸(줌 스크린샷으로 확인), 백엔드 `/analytics/assets` 직접 호출로 `valued_at: null` 확인. 이력 섹션·안내 문구도 함께 사라짐.
- ✅ AC-6: 캘린더 필터 안내 — 캘린더 뷰에서 (a) 구분=지출: "지출 필터가 적용된 거래만 합산하고 있어요" 표시, (b) 구분=지출+카테고리=식비: "지출 · 식비 필터가…" 표시, (c) 구분=전체+카테고리=식비: "식비 필터가…" 표시, (d) 둘 다 전체: 미표시 — 4가지 상태 모두 토글하며 확인.
- ✅ AC-7: 회귀 없음 — upsert 기록 흐름(AC-2에서 PUT 200), 캘린더 렌더·월 이동 헤더·오늘 표시, 테이블 뷰·빈 상태 문구 정상. diff가 가드 1줄 + 추가 전용(이력 섹션, 안내 문구)임을 코드로 확인. 캘린더 날짜 선택 로직은 미변경(거래 데이터 부재로 합계 표시는 미실측).
- ✅ AC-8: `npm run build` 통과(rolldown-vite, 기존 echarts 500kB 청크 경고만), `npm run lint` 통과(에러 0, 기존 `useReactTable` react-compiler 경고 1건 — 변경 파일과 무관한 기존 경고).

## 검증 시나리오

- 정적: 변경 2개 파일 전체 + `frontend/src/lib/api.ts`, `frontend/src/types.ts`(Valuation 기존재), `backend/app/routers/valuations.py`(GET/DELETE 기존재) 추적. 백엔드 diff 없음 확인(변경 금지 준수).
- 빌드/린트: `npm run build`, `npm run lint` (frontend) — 둘 다 통과.
- 백엔드 API(직접 호출): 임시 계정 생성 → 평가액 2건 PUT → GET 이력(내림차순) → 타 계정 id로 DELETE 시 404(교차 계정 보호) → 개별 DELETE 시 직전 평가액 기준 잔액 → 전체 삭제 시 `balance=opening_balance`, `valued_at=null`.
- 브라우저 E2E(Chrome, localhost:3000): AC-1~AC-6 시나리오 전부 실제 클릭·스크린샷·네트워크 로그로 검증 (위 채점 근거 참조).
- 정리: 임시 계정(id=12) 및 평가액 전부 삭제, DB를 원래 2계정 상태로 복원. 레포 파일은 본 보고서 외 미변경.

## 발견 이슈

- [Low] `frontend/src/pages/AssetsPage.tsx:95-107` — 이력 조회 응답 경합 가드 없음. 시세형 계정 A의 다이얼로그를 열었다 닫고 곧바로 계정 B를 열면, 늦게 도착한 A의 GET 응답이 B의 이력 목록을 덮어쓸 수 있음(요청 취소/타겟 일치 확인 없음). 잘못 표시된 항목을 삭제해도 서버가 계정-평가액 소유를 검증해 404를 반환하므로 데이터 손상은 없음(직접 확인) — 표시 혼동에 한정. 재현: 두 시세형 계정에서 다이얼로그 빠른 연속 오픈 + 네트워크 지연.
- [Low] `frontend/src/pages/AssetsPage.tsx:117` — 삭제 실패 에러가 원문 메시지만 노출(`(e as Error).message`). 조회 실패의 "이력을 불러오지 못했습니다: …" 같은 맥락 접두어가 없어 무엇이 실패했는지 다이얼로그만 봐서는 불명확. 동작 결함은 아님.
- [참고/환경] 레포 루트에 `2025-06-11~2026-06-11.xlsx`(untracked)가 QA 세션 중(11:59) 생성됨. 본 QA의 명령·앱 기능(xlsx 내보내기 없음)과 무관한 외부 브라우저 다운로드로 추정. 사용자 파일일 수 있어 삭제하지 않고 보고만 함 — 커밋 전 이동/삭제 및 `.gitignore` 검토 권장.

## 수정 Action Items (FAIL/CONDITIONAL 시)

- 해당 없음 (Low 이슈는 후속 개선 후보: 이력 조회에 타겟 일치 가드 추가, 삭제 실패 에러에 맥락 접두어 추가)

## 다음 단계

/git-commit 진행 가능. (커밋 전 레포 루트의 외부 유입 파일 `2025-06-11~2026-06-11.xlsx` 처리 권장)
