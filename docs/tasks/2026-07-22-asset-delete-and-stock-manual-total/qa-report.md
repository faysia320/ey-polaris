# QA Report: 자산 내역 삭제 기능 · 주식 엑셀 제외 및 총합 직접 입력

- 날짜: 2026-07-22
- 작업 폴더: `docs/tasks/2026-07-22-asset-delete-and-stock-manual-total`
- 판정: **PASS**
- 비고: 이 보고서는 2차 QA(재검증). 1차 QA에서 FAIL 사유였던 AC-10(모바일 375px 삭제 버튼 잘림)을 포함해 AC 10건 전부를 재검증했다. 이전 `qa-report.md`(12:14)는 본 문서로 대체된다.

## 데이터 영향

**실데이터 손상 없음.** 검증 전후 상태가 수치로 완전히 일치한다.

| 항목 | 검증 전 | 검증 후 |
| --- | --- | --- |
| accounts 총 개수 | 28 | 28 |
| 총자산 | 381,657,436원 | 381,657,436원 |
| stock/real_estate 평가액 | 5건(아래 목록) | 5건(동일) |
| 고아 평가액 | 0 | 0 |

평가액 5건은 `주식(영이) 2026-07-21 4,494,000` / `주식(으니) 2026-06-17 25,343,381` / `주식(으니) 2026-07-21 17,163,436` / `오산시티자이2단지 2026-06-17 355,000,000` / `오산시티자이2단지 2026-07-21 360,000,000`로 검증 전후 완전히 동일하다.

파괴적 기능(엑셀 가져오기 = 대상 월 delete-then-insert) 검증은 **실데이터가 없는 2019-01**을 대상으로만 수행했고, 실행 전 API·SQL 양쪽으로 해당 월 거래 0건을 확인했다. 검증 중 생성한 파생물(거래 1건, 계정 `QA검증계정`/`QA삭제테스트계정`/`QA엣지테스트`, 카테고리 `QA검증대분류 > QA검증소분류`, 평가액 3건)은 전부 삭제하고 0건으로 확인했다.

검증 도중 카드 탐색 실수로 실계정 `주식(으니)`의 평가액 갱신 다이얼로그를 **연 적이 있으나 저장하지 않고 닫았고**, 위 표대로 값이 변경되지 않았음을 DB로 확인했다.

## 성공 기준 채점

- ✅ **AC-1 (자산 계정 삭제 진입점)**: 375px 자산 페이지 DOM에서 `button[aria-label$="삭제"]` **27개**를 계정 카드마다 검출. 데스크톱 스크린샷에서도 각 카드 우상단 휴지통 아이콘 확인.
- ✅ **AC-2 (계정 삭제 확인 절차)**: `QA삭제테스트계정` 삭제 클릭 → 다이얼로그 문구 확인 = "QA삭제테스트계정 계정을 삭제할게요. 이 계정의 평가 이력도 함께 삭제되며 되돌릴 수 없어요." (대상 계정명 + 평가 이력 동반 삭제 + 되돌릴 수 없음 모두 충족). `fetch`를 후킹해 취소 클릭 후 **DELETE 요청 0건**, 계정 카드 잔존, 다이얼로그는 스크린샷상 화면에서 사라짐을 확인.
- ✅ **AC-3 (삭제 성공 반영)**: 평가액만 있고 거래가 없는 `QA삭제테스트계정`(잔액 1,234,000원) 삭제 → 총자산 표시가 **382,890,436원 → 381,656,436원**으로 정확히 1,234,000원 감소. 네트워크상 `DELETE /accounts/134` → `GET /accounts` → `GET /analytics/assets` 재조회 확인, 카드 즉시 소멸. 평가액 2건도 CASCADE로 함께 삭제됨(잔여 0건, 고아 0건).
- ✅ **AC-4 (삭제 차단 에러 처리)**: 거래 1건이 참조 중인 `QA검증계정` 삭제 시도 → 다이얼로그 안에 백엔드 409 원문 "거래에서 참조 중인 계정은 삭제할 수 없습니다 (비활성화를 사용하세요)" 표시, **다이얼로그 유지**(`data-state="open"`), 계정 잔존, 총자산 불변(382,890,436원), 페이지 정상.
- ✅ **AC-5 (평가액 삭제 확인)**: 평가 이력 삭제 버튼 → 중첩 확인 다이얼로그 2개 스택 확인, 문구 "2026-07-22 기준 5,000,000원 기록을 삭제할게요. 되돌릴 수 없어요." 취소 시 **DELETE 0건·이력 잔존**. 확정 시 `DELETE /accounts/134/valuations/41` 1회, 확인 다이얼로그만 닫히고 갱신 다이얼로그는 유지, 이력 목록 2건으로 갱신, 총자산 386,656,436 → 382,890,436원으로 복귀.
- ✅ **AC-6 (엑셀 주식 제외 — 파싱)**: 합성 xlsx 4케이스를 컨테이너 내에서 `parse_valuations` 직접 호출로 검증. ① 주식 2행+부동산 2행(엑셀 병합 carry-forward 모사) → **부동산 2건만** 반환 ② 주식만 있는 시트 → `[]` ③ 주식 0원+부동산 0원 → 부동산 1건만 ④ 비xlsx 바이트 → `[]`. `VALUATION_ITEM_TYPES == {'부동산': 'real_estate'}` 확인.
- ✅ **AC-7 (엑셀 주식 제외 — 미리보기·확정)**: 실제 API 검증.
  - 미리보기: 주식 행(`주식(으니)`, `삼성전자`) + 부동산 행 중 **기존 stock 계정과 동명인 `주식(영이)`** 를 섞은 픽스처로 `POST /transactions/import/preview` → `valuations`에 `오산시티자이2단지` 1건만. 동명 stock 계정 매칭 차단(`account.type != "real_estate"`)이 실제로 동작.
  - 확정: 매칭 대상이 없는 픽스처로 `POST /transactions/import` (2019-01, 사전 0건 확인) → `valuation_count: 0`, `created_accounts: ["QA검증계정"]`(주식 계정 신규 생성 없음), stock 계정 수 2 유지, 평가액 5건 전부 변경 없음.
  - `guess_account_type()`이 `stock`을 반환하는 분기가 없음을 소스로 확인 — 거래 시트 경로로도 주식 계정이 자동 생성될 수 없다.
- ✅ **AC-8 (주식 총합 직접 입력)**: stock 계정 카드 버튼 라벨이 `총합 입력`(비주식은 `평가액 갱신`), 다이얼로그 제목 `보유 주식 총합 입력 — <계정명>`, 설명 "기준일의 보유 주식 평가 총합을 직접 입력해요. 주식은 엑셀 업로드로 갱신되지 않아요.", 입력 라벨 `보유 주식 총합 (원)`. 실제 저장(2026-07-22 / 5,000,000원) 후 카드가 `5,000,000원 / 평가 기준일 2026-07-22`로, 총자산이 382,890,436 → **386,656,436원(+3,766,000 = 5,000,000−1,234,000)** 으로 정확히 갱신됨.
- ✅ **AC-9 (타입·빌드 정합성)**: `npm run build` → `✓ built in 1.34s`, 오류 0, 번들 `index-Dh6wmbNW.js`. `npm run lint` → **0 errors, 2 warnings**. 경고 2건은 `TransactionsPage.tsx:329,332`(useReactTable 관련)로 **이번 diff가 건드리지 않은 라인**임을 diff 대조로 교차 확인 — 기존 경고로 채택.
- ✅ **AC-10 (모바일 375px)**: 레시피 A(iframe 실측)로 직접 측정. **1차 FAIL 지점이 해소됨**을 수치로 확인.
  - 페이지 오버플로: `/assets` `/transactions` `/` 모두 `pageHasHorizontalScroll: false`, `unclippedOffenders: []`.
  - 계정 카드 전수(27개): `CardTitle` 그리드 트랙이 **264px**(1차 실패 시 293.4px)로 카드 가용 폭 내로 제한됨. **삭제 버튼 잘림 0/27건**(`btnRight 312 ≤ cardRight 328`). 1차 실패 카드 `우리직장인재테크 저축예금(수수료우대형`에서 계정명 span이 실제로 `scrollWidth − clientWidth = 29px` 초과분을 ellipsis 처리(`nameTruncated: true`)하며 버튼은 카드 안에 있음 → `min-w-0` 수정이 의도대로 작동.
  - 계정 삭제 확인 다이얼로그: `left 25 / right 351 / width 326`, 뷰포트 내, 오버플로 0, 취소·삭제 버튼 각 296×30px.
  - 평가액 갱신 다이얼로그 + 중첩 평가액 삭제 확인 다이얼로그: 둘 다 `width 326`, 뷰포트 내, 오버플로 0, 버튼 296×30px.
  - 터치 크기: 확인 다이얼로그의 삭제/취소 버튼은 296px 폭으로 충분. 카드의 아이콘 버튼은 28×28px이며 이는 앱 전역 `size="icon-sm"` 관례와 동일 — 아래 [Low] 1 참조.

## 검증 시나리오

**실행 환경**: 기동 중인 `ey-polaris-*` 스택 재사용. 서빙 번들(`curl localhost:3000` → `index-Dh6wmbNW.js`)이 방금 빌드한 산출물과 일치, 백엔드 컨테이너 소스도 현재 작업 트리와 일치함을 확인(`VALUATION_ITEM_TYPES = {"부동산": "real_estate"}`, `account.type != "real_estate"`) — 재빌드 없이 최신 코드 검증.

**정적 분석**: 변경 6개 파일 전체 diff + 주변 맥락 정독. 1-hop 추적으로 `stores/masterData.ts:81-84`(`deleteAccount`), `routers/valuations.py`(PUT upsert / DELETE), `excel_import.guess_account_type`, `components/ui/button.tsx`(`icon-sm`) 확인.

**동적 검증 (실행 명령)**:
1. `cd frontend && npm run build` / `npm run lint`
2. 컨테이너 내 `parse_valuations` 4케이스 직접 호출(합성 xlsx, 픽스처는 레포 밖 스크래치패드에서 생성)
3. `POST /transactions/import/preview` — 비파괴, 실행 전후 계정·평가액 상태 동일 확인
4. `POST /transactions/import` (2019-01, 사전 0건 확인 후) — `valuation_count: 0` 확인
5. 브라우저 E2E: AC-1~5, AC-8 전체 흐름을 `fetch` 후킹으로 요청 단위까지 검증
6. 레시피 A iframe 375px 실측 — 3개 페이지 + 카드 27개 + 다이얼로그 3종

**시도한 엣지 케이스**:
- 평가액 빈 값 저장 → `기록` 클릭 시 다이얼로그 유지 + "평가액을 입력해주세요" 표시 ✅
- 미래 기준일 → 백엔드 `PUT` 직접 호출로 **422** + "평가 기준일은 미래 날짜일 수 없습니다" 확인. 프런트는 `DatePicker disableFuture`로 선택 자체를 차단(초기 UI 시도는 커스텀 컴포넌트라 프로그래매틱 값 주입이 React 상태에 닿지 않아 무효 — 하네스 한계이지 제품 결함 아님) ✅
- 음수 평가액 → 백엔드 422 ✅
- 다른 계정의 평가액 id로 삭제 → 404 ✅
- 엑셀 부동산 행이 기존 **stock** 계정과 동명인 충돌 케이스 → 미리보기·확정 모두에서 제외 ✅
- 주식만 있는 시트 / 0원 행 / 비xlsx 입력 → 모두 안전하게 빈 결과 ✅
- 삭제 취소 경로(계정·평가액 양쪽) → DELETE 요청 0건 ✅

**하네스 한계 (제품 결함 아님)**: 자동화로 다이얼로그를 닫으면 Radix exit 애니메이션의 `animationend`가 발화하지 않아 `[role="dialog"][data-state="closed"]` 노드가 DOM에 남는다. 스크린샷으로 **화면상 완전히 닫혀 있고 페이지가 정상 조작 가능함**을 확인해 측정 아티팩트로 판정했다.

## 발견 이슈

- **[Low] 카드 삭제 아이콘 버튼 터치 타깃 28×28px** — `frontend/src/pages/AssetsPage.tsx:176` (`size="icon-sm"`). Apple HIG 44px / Material 48px 권고 대비 작다. 다만 `SettingsPage.tsx:602,607`, `TransactionsPage.tsx:313,318,752,757`, `BudgetsPage.tsx:222`, `month-picker.tsx`, `dialog.tsx:75` 등 **앱 전역이 동일한 `icon-sm` 관례**를 쓰고 있어 이번 변경이 만든 회귀가 아니다. AC-10이 명시한 "삭제/취소 버튼"(확인 다이얼로그의 296px 버튼)은 충족한다. 관례를 바꾸려면 전역 디자인 결정이 필요하므로 별도 작업 권장.
- **[Low] `deleteAccount`가 삭제 실패와 재조회 실패를 구분하지 못함** — `frontend/src/stores/masterData.ts:81-84`. `api.delete` 성공 후 `fetchAll()`이 실패하면 `confirmAccountDelete`의 catch로 떨어져, 실제로는 삭제됐는데 다이얼로그가 열린 채 "삭제 실패"처럼 보인다(재시도 시 404). 구현자가 self-report한 항목이며 코드로 재확인함. 발생 확률 낮음.
- **[Low] 중첩 다이얼로그 방어 코드의 무피드백 no-op** — `frontend/src/pages/AssetsPage.tsx` `confirmValuationDelete`의 `if (!valuationTarget || !valuationToDelete) return;`. 바깥 평가액 갱신 다이얼로그가 닫히고 안쪽 확인 다이얼로그만 남는 상태가 되면 "삭제" 버튼이 아무 피드백 없이 동작하지 않는다. Radix dismissable-layer 스택 특성상 일반 조작으로는 도달하기 어려워 실사용 영향은 낮다.
- **[Low] `stock-consolidation.sql`이 환경 재현 불가** — `docs/tasks/.../stock-consolidation.sql`. 개발 DB에 1회 적용된 데이터 변경으로 Alembic 마이그레이션이 아니다. 다른 환경에서는 기존 주식 계정 10개가 그대로 남아 이 DB와 상태가 갈린다. research.md가 자동 마이그레이션을 명시적으로 범위 밖에 뒀고 SQL 원문·적용 결과가 기록되어 있어 추적은 가능하다. 스크립트 자체는 검토 결과 안전(대상 스냅샷을 TEMP 테이블로 고정해 신규 계정 오염을 방지, 날짜별 합계 이관으로 추이 보존, 트랜잭션 래핑).

**High: 0건 / Medium: 0건 / Low: 4건**

## QA 중 적용한 수정 (Low 한정)

**없음.** 발견한 Low 4건은 모두 /qa의 수정 허용 범위(오타·문서-코드 불일치·미사용 변수·국소적 스타일 보정 등 **동작을 바꾸지 않는** 변경)를 벗어난다.

- Low 1(터치 타깃)은 앱 전역 디자인 관례 변경이라 국소 수정이 아니다.
- Low 2·3은 동작 변경(에러 처리 분기 재설계)에 해당한다.
- Low 4는 마이그레이션 전략 결정 사항이다.

`implementation.md`·`research.md`·`stock-consolidation.sql`의 서술은 수치를 포함해 대조했고(예: 주식 합계 4,494,000 + 17,163,436 = 21,657,436, 스냅샷 2건 보존, 총자산 381,657,436원) **사실 오류를 발견하지 못해** 정정할 문서 항목도 없었다.

## 수정 Action Items

Medium/High 이슈가 없어 `/implement` 재실행이 필요한 항목은 없다. 아래는 별도 작업으로 분리 권장:

- [ ] (선택) 아이콘 버튼 터치 타깃을 앱 전역에서 상향(`icon-sm` 28px → 최소 44px 히트 영역) — 디자인 결정 필요, 전 페이지 영향
- [ ] (선택) `masterData` 스토어의 mutate/refetch 실패 구분 — 전 페이지 공통 패턴 변경
- [ ] (선택) 기존 주식 계정 통합을 Alembic 데이터 마이그레이션으로 승격할지 결정

## 다음 단계

`/git-commit` 진행 가능. (`docs/tasks/2026-07-22-asset-delete-and-stock-manual-total/`는 untracked이므로 `stock-consolidation.sql`을 포함해 커밋 대상에 포함할지 확인 필요)
