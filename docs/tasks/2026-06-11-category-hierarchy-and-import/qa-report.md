# QA Report: 카테고리 대/소분류 개편 + 뱅크샐러드 엑셀 월별 업로드

- 날짜: 2026-06-11
- 작업 폴더: docs/tasks/2026-06-11-category-hierarchy-and-import
- 판정: PASS

## 성공 기준 채점

- ✅ AC-1: `GET /api/v1/categories` 60건(지출 56 + 수입 4, '환불>미분류' 포함). 명세의 엑셀 전수 표와 쌍 단위로 대조 — 누락 0건, 초과 0건. fixed 9건도 명세 제안 목록과 정확히 일치. 미참조 '교통>미분류'는 정리되어 56쌍(57 아님)으로 확인.
- ✅ AC-2: 마이그레이션 전 생성된 수동 거래(id=11, memo '마이그레이션 보존 테스트')가 '생활 > 생필품'으로 재매핑되어 조회되고, 예산 1건(2026-06 '생활 > 생필품' 100,000원)도 동일하게 잔존. (마이그레이션 자체는 이미 적용된 상태라 전/후 재현은 불가 — 잔존 증거로 확인)
- ✅ AC-3: 중복 생성 409("이미 존재하는 카테고리입니다: 식비 > 미분류"), 중복으로의 수정 409, 거래 참조 중 삭제 409, 신규 생성→삭제 라운드트립(201→204) 모두 REST로 직접 확인.
- ✅ AC-4: `month=2026-05` 업로드 → deleted=65/created=65/skipped=27(이체, 사유 보고). 생성+스킵=92로 엑셀 실측(지출61+수입4+이체27=92행)과 일치 — 엑셀 원본을 컨테이너에서 직접 파싱해 대조.
- ✅ AC-5: `month=2026-04` 업로드 후 환불 7건이 kind=income·카테고리 '환불'·memo `[환불: 원분류]` 형식으로 등록. 엑셀 실측도 2026-04 환불(지출+양수) 7건 — **명세의 "16건"은 명세 측 집계 오류**(연간 48건은 실측 일치). 명세 의도(환불→수입 반영·원분류 보존)는 전수 충족.
- ✅ AC-6: 동일 파일 2회 연속 업로드 → 매회 deleted=65/created=65, 총 건수 66(가져오기 65 + 수동 1) 불변, 수동 거래 보존 확인.
- ✅ AC-7: 파일에만 있던 결제수단 8종(ALL 우리카드 Infinite, WON 통장, 네이버페이 간편결제(+포인트), 카카오페이 간편결제, 카드의정석 후불하이패스+, 토스 간편결제, Trip to 로카)이 Account로 자동 생성·거래 연결. (명세 "11종"은 연간 전체 기준 추정 — 가져온 3개월 범위에서 등장한 결제수단은 전부 생성됨. income/expense 행 기준 연간 실측은 10종)
- ✅ AC-8: 비-xlsx 파일·시트 부재·필수 컬럼 누락·범위 밖 월(2024-01)·잘못된 월 형식(2026-13) 모두 422 + 한국어(형식 오류는 pydantic 표준) 메시지, 기존 거래 건수 불변 확인. 검증 픽스처는 OS 임시 디렉터리에 생성 후 정리.
- ✅ AC-9: 브라우저(localhost:3000)에서 거래 폼 대분류→소분류 2단계 셀렉트로 거래 생성('식비>배달' 12,345원 → API로 존재 확인 후 삭제), 목록 필터 대분류(여행/숙박, 15건=API와 일치)→소분류(숙박비, 5건) 연동 확인.
- ✅ AC-10: 설정 페이지 카테고리 탭 — 대분류/소분류 컬럼(연속 그룹 표시), 추가 다이얼로그(대분류 datalist+소분류+구분+성격) 렌더링 확인. 생성/수정/삭제 동작은 AC-3 API 검증으로 충족.
- ✅ AC-11: 업로드 다이얼로그에 실제 파일·월(2026-05) 지정 후 업로드 → "가져오기 완료 — 등록 65건 (기존 65건 교체), 건너뜀 27건" 요약과 행별 스킵 사유 목록 표시, 목록 자동 갱신 확인.
- ✅ AC-12: 대시보드(2026-04) 도넛이 대분류 11개로 집계 표시(식비, 여행/숙박, 금융 등 — API 집계와 일치), 예산 페이지 categoryLabel 표시·기존 예산(생활>생필품 100,000원) 저장/삭제 UI 회귀 없음.
- ✅ AC-13: `npm run build` 통과(청크 경고는 기존 echarts), `npm run lint` 0 errors(경고 2건은 기존 TanStack Table 패턴), backend 컨테이너 정상 기동(alembic head=0004) + 전체 API 응답 정상.

## 검증 시나리오

- 정적: 변경 16파일 + 신규 2파일(마이그레이션 0004, excel_import.py) 전체 정독. 마이그레이션 재매핑 표가 research 표와 일치함을 대조, 재매핑 시 예산 유니크 충돌 불가(수입 매핑만 동일 대상 공유, 예산은 지출 한정) 확인.
- 엑셀 원본 실측: 컨테이너 내 openpyxl로 직접 파싱 — 2026-05 92행, 2026-04 149행(환불 7), 지출 56쌍·수입 3쌍, 연간 환불 48건. 명세 표와 시드의 독립 대조 근거로 사용.
- API: 카테고리 CRUD(409 3종 + 라운드트립), import 정상/멱등/에러 5종, major 필터, dashboard 집계, accounts/budgets/transactions 조회.
- 브라우저(Chrome 자동화): 거래 필터·2단계 폼·실제 거래 생성/삭제, 업로드 다이얼로그 전체 플로우(파일 주입→결과 요약→갱신), 설정/대시보드/예산 페이지.
- 빌드: `npm run build`, `npm run lint`.
- 검증 중 생성한 데이터(QA 카테고리, 테스트 거래)는 전부 삭제했고 업로드는 멱등 교체라 DB 상태는 구현 종료 시점과 동일. 레포 작업 트리 무변경(`git status` 대조) 확인.

## 발견 이슈

- [Low] `backend/app/routers/analytics.py:14` — `_month_range`가 `transactions.py:14`와 완전 중복. implementation.md는 "헬퍼 추출"이라 기술했으나 analytics에는 사본이 남아 있음. 동작 문제는 없으나 한쪽(예: utils)으로 통합 권장.
- [Low] `backend/app/routers/categories.py:15-17` — 카테고리 정렬이 DB 컬레이션 특성상 가나다순이 아님(실측: 글자수 우선 정렬 — 교통→구독→금융→생활→식비→미분류→… 순). 대분류 그룹 연속성은 유지되어 SettingsPage 그룹 표시는 정상이나, 사용자 기대(가나다)와 다름. 프론트 정렬 또는 `ko` 컬레이션 적용 고려. (implementation.md도 인지·허용으로 기록)
- [Low] `backend/app/routers/transactions.py:115-142` — 사용자가 가져오기 거래를 수정해도 source='import'가 유지되므로, 같은 월 재업로드 시 수정 내용이 통째로 교체·소실됨. AC-6 계약(수동 거래만 보존)과 다이얼로그 경고 문구에는 부합하므로 결함은 아니나, TransactionOut에 source 미노출이라 사용자가 어떤 거래가 교체 대상인지 UI에서 구분할 수 없음. 추후 source 노출 또는 수정 시 안내 고려.
- [Low] `frontend/src/pages/TransactionsPage.tsx:285-287` — 구분 필터가 '전체'일 때 소분류 필터 목록이 kind 무관 동일 major의 카테고리를 합쳐 보여줌. 수입/지출에 동명 대분류를 만들면 목록이 섞임(현 시드에서는 동명 대분류 없음 — 실해 없음).
- [Low] `backend/app/excel_import.py:120-123` — 금액이 소수(float)면 `int()`로 조용히 절사. 원화 데이터 특성상 실데이터에는 없으나, 절사 발생 시 사유 없이 값이 변형됨. 방어적으로 스킵 또는 반올림 명시 고려.

## 수정 Action Items

- (없음 — Low 이슈는 선택 개선 사항으로 후속 작업에서 검토)

## 다음 단계

PASS — /git-commit 진행 가능. (참고: implementation.md 보류 항목대로 DB에 2026-03/04/05 가져오기 데이터·테스트 거래 1건·2026-06 예산 1건이 남아 있음. 실데이터 운영 전 정리 여부만 결정하면 됨)
